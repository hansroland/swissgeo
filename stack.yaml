# Stack file for swissgeo
resolver: nightly-2018-04-25

packages:
- '.'

# Extra packages from Hackage
extra-deps: []

# Override default flag values for local packages and extra-deps
flags: {}

# Extra package databases containing global packages
extra-package-dbs: []